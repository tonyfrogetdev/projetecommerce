<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('homepage') }}">Temple de Râ</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto">

				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Cartes</a>
					<div class="dropdown-menu">
						<a class="dropdown-item" href="{{ path('product_category',{'slug': 'deck' }) }}">Deck de démarrage</a>
						<div class="dropdown-divider"></div>
						<a class="dropdown-item" href="{{ path('product_category',{'slug': 'boosters' }) }}">Boosters de cartes</a>
						<div class="dropdown-divider"></div>

						<a class="dropdown-item" href="{{ path('product_category',{'slug': 'speedduel' }) }}">Speed Duel</a>


					</div>
				</li>
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Accessoires</a>
					<div class="dropdown-menu">
						<a class="dropdown-item" href="{{ path('product_category',{'slug': 'rangements' }) }}">Rangements</a>
						<div class="dropdown-divider"></div>
						<a class="dropdown-item" href="{{ path('product_category',{'slug': 'sleeves' }) }}">Sleeves</a>
						<div class="dropdown-divider"></div>

						<a class="dropdown-item" href="{{ path('product_category',{'slug': 'figurines' }) }}">Figurines</a>

<div class="dropdown-divider"></div>

						<a class="dropdown-item" href="{{ path('product_category',{'slug': 'goodies' }) }}">Goodies</a>
					</div>
				</li>
			</ul>

		</div>


		<ul class="navbar-nav">
			{% if is_granted('ROLE_ADMIN') %}
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Menu Admin</a>
					<div class="dropdown-menu">
						<a href="{{ path('product_create')}}" class="dropdown-item">Créer une nouvelle catégorie</a>
						<div class="dropdown-divider"></div>
						<a href="{{ path('category_create')}}" class="dropdown-item">Créer un nouveau produit</a>
						<div class="dropdown-divider"></div>
						<a href="{{ path('inscription_liste') }}" class="dropdown-item">Liste des membres</a>


					</div>

				</li>


			{% endif %}
			<li class="nav-item">
				<a href="{{ path('cart_show') }}" class="nav-link">
					<i class="fas fa-shopping-cart"></i>
					Panier ({{ cartService.total / 100 }}
					€)</a>
			</li>

			{% if app.user %}

				<li class="nav-item">
					<a href="{{ path('purchase_index') }}" class="nav-link">Mes commandes</a>
				</li>

				<li class="nav-item">
					<a href="{{ path('security_logout') }}" class="btn btn-danger">Déconnexion</a>
				</li>


			{% else %}


				<li class="nav-item">
					<a href="{{ path('app_register') }}" class="btn btn-info">Inscription</a>
				</li>
				<li class="nav-item">
					<a href="{{ path('security_login') }}" class="btn  btn-success">Connexion</a>
				</li>
			{% endif %}


		</ul>
	</div>

</nav>
